# HTTP Server metrics as of 24/04/2025

The following attributes relate to the specified metrics:

| Metric                           | Attribute                   | Type   | Description                                                                                              | Examples                                                                        | Requirement Level                                            | Stability   |
| -------------------------------- | --------------------------- | ------ | -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | ------------------------------------------------------------ | ----------- |
| `http.server.request.duration`   | `http.request.method`       | string | HTTP request method. [1]                                                                                 | `GET`; `POST`; `HEAD`                                                           | Required                                                     | Stable      |
| `http.server.request.duration`   | `url.scheme`                | string | The URI scheme component identifying the used protocol. [2]                                              | `http`; `https`                                                                 | Required                                                     | Stable      |
| `http.server.request.duration`   | `error.type`                | string | Describes a class of error the operation ended with. [3]                                                 | `timeout`; `java.net.UnknownHostException`; `server_certificate_invalid`; `500` | Conditionally Required If request has ended with an error.   | Stable      |
| `http.server.request.duration`   | `http.response.status_code` | int    | HTTP response status code.                                                                               | `200`                                                                           | Conditionally Required If and only if one was received/sent. | Stable      |
| `http.server.request.duration`   | `http.route`                | string | The matched route, that is, the path template in the format used by the respective server framework. [4] | `/users/:userID?`; `{controller}/{action}/{id?}`                                | Conditionally Required If and only if it's available         | Stable      |
| `http.server.request.duration`   | `network.protocol.name`     | string | OSI application layer or non-OSI equivalent. [5]                                                         | `http`; `spdy`                                                                  | Conditionally Required [6]                                   | Stable      |
| `http.server.request.duration`   | `network.protocol.version`  | string | The actual version of the protocol used for network communication. [7]                                   | `1.0`; `1.1`; `2`; `3`                                                          | Recommended                                                  | Stable      |
| `http.server.request.duration`   | `server.address`            | string | Name of the local HTTP server that received the request. [8]                                             | `example.com`; `10.1.2.80`; `/tmp/my.sock`                                      | Opt-In                                                       | Stable      |
| `http.server.request.duration`   | `server.port`               | int    | Port of the local HTTP server that received the request. [9]                                             | `80`; `8080`; `443`                                                             | Opt-In                                                       | Stable      |
| `http.server.request.duration`   | `user_agent.synthetic.type` | string | Specifies the category of synthetic traffic, such as tests or bots. [10]                                 | `bot`; `test`                                                                   | Opt-In                                                       | Development |
| `http.server.active_requests`    | `http.request.method`       | string | HTTP request method. [1]                                                                                 | `GET`; `POST`; `HEAD`                                                           | Required                                                     | Stable      |
| `http.server.active_requests`    | `url.scheme`                | string | The URI scheme component identifying the used protocol.                                                  | `http`; `https`                                                                 | Required                                                     | Stable      |
| `http.server.active_requests`    | `server.address`            | string | Name of the local HTTP server that received the request. [2]                                             | `example.com`; `10.1.2.80`; `/tmp/my.sock`                                      | Opt-In                                                       | Stable      |
| `http.server.active_requests`    | `server.port`               | int    | Port of the local HTTP server that received the request. [3]                                             | `80`; `8080`; `443`                                                             | Opt-In                                                       | Stable      |
| `http.server.request.body.size`  | `http.request.method`       | string | HTTP request method. [1]                                                                                 | `GET`; `POST`; `HEAD`                                                           | Required                                                     | Stable      |
| `http.server.request.body.size`  | `url.scheme`                | string | The URI scheme component identifying the used protocol. [2]                                              | `http`; `https`                                                                 | Required                                                     | Stable      |
| `http.server.request.body.size`  | `error.type`                | string | Describes a class of error the operation ended with. [3]                                                 | `timeout`; `java.net.UnknownHostException`; `server_certificate_invalid`; `500` | Conditionally Required If request has ended with an error.   | Stable      |
| `http.server.request.body.size`  | `http.response.status_code` | int    | HTTP response status code.                                                                               | `200`                                                                           | Conditionally Required If and only if one was received/sent. | Stable      |
| `http.server.request.body.size`  | `http.route`                | string | The matched route, that is, the path template in the format used by the respective server framework. [4] | `/users/:userID?`; `{controller}/{action}/{id?}`                                | Conditionally Required If and only if it's available         | Stable      |
| `http.server.request.body.size`  | `network.protocol.name`     | string | OSI application layer or non-OSI equivalent. [5]                                                         | `http`; `spdy`                                                                  | Conditionally Required [6]                                   | Stable      |
| `http.server.request.body.size`  | `network.protocol.version`  | string | The actual version of the protocol used for network communication. [7]                                   | `1.0`; `1.1`; `2`; `3`                                                          | Recommended                                                  | Stable      |
| `http.server.request.body.size`  | `server.address`            | string | Name of the local HTTP server that received the request. [8]                                             | `example.com`; `10.1.2.80`; `/tmp/my.sock`                                      | Opt-In                                                       | Stable      |
| `http.server.request.body.size`  | `server.port`               | int    | Port of the local HTTP server that received the request. [9]                                             | `80`; `8080`; `443`                                                             | Opt-In                                                       | Stable      |
| `http.server.request.body.size`  | `user_agent.synthetic.type` | string | Specifies the category of synthetic traffic, such as tests or bots. [10]                                 | `bot`; `test`                                                                   | Opt-In                                                       | Development |
| `http.server.response.body.size` | `http.request.method`       | string | HTTP request method. [1]                                                                                 | `GET`; `POST`; `HEAD`                                                           | Required                                                     | Stable      |
| `http.server.response.body.size` | `url.scheme`                | string | The URI scheme component identifying the used protocol. [2]                                              | `http`; `https`                                                                 | Required                                                     | Stable      |
| `http.server.response.body.size` | `error.type`                | string | Describes a class of error the operation ended with. [3]                                                 | `timeout`; `java.net.UnknownHostException`; `server_certificate_invalid`; `500` | Conditionally Required If request has ended with an error.   | Stable      |
| `http.server.response.body.size` | `http.response.status_code` | int    | HTTP response status code.                                                                               | `200`                                                                           | Conditionally Required If and only if one was received/sent. | Stable      |
| `http.server.response.body.size` | `http.route`                | string | The matched route, that is, the path template in the format used by the respective server framework. [4] | `/users/:userID?`; `{controller}/{action}/{id?}`                                | Conditionally Required If and only if it's available         | Stable      |
| `http.server.response.body.size` | `network.protocol.name`     | string | OSI application layer or non-OSI equivalent. [5]                                                         | `http`; `spdy`                                                                  | Conditionally Required [6]                                   | Stable      |
| `http.server.response.body.size` | `network.protocol.version`  | string | The actual version of the protocol used for network communication. [7]                                   | `1.0`; `1.1`; `2`; `3`                                                          | Recommended                                                  | Stable      |
| `http.server.response.body.size` | `server.address`            | string | Name of the local HTTP server that received the request. [8]                                             | `example.com`; `10.1.2.80`; `/tmp/my.sock`                                      | Opt-In                                                       | Stable      |
| `http.server.response.body.size` | `server.port`               | int    | Port of the local HTTP server that received the request. [9]                                             | `80`; `8080`; `443`                                                             | Opt-In                                                       | Stable      |
| `http.server.response.body.size` | `user_agent.synthetic.type` | string | Specifies the category of synthetic traffic, such as tests or bots. [10]                                 | `bot`; `test`                                                                   | Opt-In                                                       | Development |
